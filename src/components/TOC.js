import React, { Component } from 'react';

class TOC extends Component {

    render() {
        var lists = {};
        var data = this.props.data;
        var i = 0;
        while(i< data.length){
        lists.push()
        }  

        return (
            <nav>
                <ul>
                </ul>

            </nav>
        );
    }
}

export default TOC;